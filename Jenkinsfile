pipeline{
    agent any 
    stages{
        stage ("code"){
            steps{
                sh 'this is code'
            }
        }
        stage ("build"){
            steps{
                sh 'this is build'
            }
        }
    }
}
